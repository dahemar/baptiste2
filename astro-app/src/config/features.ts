/**
 * Feature flags — flip without deleting underlying code.
 * Re-enable theatre desktop "files" playlist: set THEATRE_FILES_VIEW_ENABLED to true.
 */
export const THEATRE_FILES_VIEW_ENABLED = false;
